#include <stdio>

/**
 * main - program that prints in sigle number
 * followed by a new line
 * Return: 0 successfully
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
