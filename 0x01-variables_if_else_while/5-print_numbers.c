#include <stdio.h>

/**
 * main - print all the single numbers
 * followed by a new line
 * Return: 0 successfully
 */
int main(void)
{
	int n = 0

	while (n < 10)
	{
		printf("%d\n", n);
		n++;
	}
	return (0);
}
