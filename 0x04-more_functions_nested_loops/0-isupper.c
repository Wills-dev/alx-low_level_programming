#include <stdio.h>
#include "main.h"

/**
 * _isupper - prints upper case
 * @c: Return 1 if c is uppercase
 * Return: 0 if otherwise
 */
int _isupper(int c);
{
	char ch;

	for (ch = 'A'; ch <= 'Z'; ch++)
	{
		if (c == 'C')
		{
			return (1);
		}
	}
	return (0);
}
